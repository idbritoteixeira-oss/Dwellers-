buildscript {
    ext.kotlin_version = '1.9.0' // ATUALIZADO AQUI
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        // Versão do Gradle compatível com Kotlin 1.9
        classpath "com.android.tools.build:gradle:8.1.0" 
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
