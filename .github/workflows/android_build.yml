      - name: Build APK
        run: |
          flutter pub get
          # Este comando força o download de todos os artefatos do motor antes do build
          flutter precache --android
          # Build sem o tree-shake para evitar erros de recursos
          flutter build apk --release --no-tree-shake-icons
          